{
  "buildCommand": "bun install && bunx vite build --config vite.spa.config.ts",
  "outputDirectory": "dist-spa",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
